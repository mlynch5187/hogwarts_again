<h1>All Professors</h1>

<% @professors.each do |professor| %>
    <section id="professors-<%=professor.id%>">
      <p><%= "#{professor.name}" %></p>
      <p><%= "#{professor.age}" %></p>
      <p><%= "#{professor.specialty}" %></p>
        <br/>
        <br/>
        <br/>
        <br/>
      </section>
    <% end %>
