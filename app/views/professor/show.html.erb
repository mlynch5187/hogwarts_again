<h1> @professor.name </h1>

<h3>All Students</h3>
  <% @professor.students.each do |student| %>
    <p>Name: <%= student.name %></p>
  <% end %>
    <p>Average Student Age: <%= @professor.average_age %></p>
